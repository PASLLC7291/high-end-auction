# Basta API Configuration
ACCOUNT_ID="<your-basta-account-id>"
NEXT_PUBLIC_ACCOUNT_ID="<your-basta-account-id>"
API_KEY="<your-basta-api-key>"
BASTA_WEBHOOK_SECRET="<your-basta-webhook-secret-or-shared-token>"
# Webhook auth:
# - If Basta provides a signing secret (whsec_...), set it here to verify x-basta-signature.
# - If you don’t have a signing secret, set a random token and configure action hooks to send:
#   x-fastbid-webhook-token: <BASTA_WEBHOOK_SECRET>
#   (helper: pnpm basta:webhooks:sync -- --url https://<your-domain>/api/webhooks/basta --apply)
# Optional: default target URL for scripts/sync-basta-webhooks.ts
# BASTA_WEBHOOK_URL="https://your-site.com/api/webhooks/basta"

# Optional: Override Basta domain/endpoints (defaults to basta.app)
# BASTA_DOMAIN="basta.app"
# NEXT_PUBLIC_BASTA_DOMAIN="basta.app"
# BASTA_CLIENT_API_URL="https://client.api.basta.app/graphql"
# NEXT_PUBLIC_BASTA_CLIENT_API_URL="https://client.api.basta.app/graphql"
# BASTA_MANAGEMENT_API_URL="https://management.api.basta.app/graphql"
# NEXT_PUBLIC_BASTA_WS_CLIENT_API_URL="wss://client.api.basta.app/graphql"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="<generate-with-openssl-rand-base64-32>"

# Database (Turso / libSQL)
# Local dev: leave empty to use SQLite file (./db/local.db)
# Deployment: set BOTH TURSO_DATABASE_URL + TURSO_AUTH_TOKEN (remote URL, not "file:")
# TURSO_DATABASE_URL="libsql://your-db.turso.io"
# TURSO_AUTH_TOKEN="<your-turso-auth-token>"

# Stripe - Required for payment features
STRIPE_SECRET_KEY="<your-stripe-secret-key>"
STRIPE_WEBHOOK_SECRET="<your-stripe-webhook-secret>"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="<your-stripe-publishable-key>"

# Analytics (optional)
# NEXT_PUBLIC_POSTHOG_KEY="<your-posthog-project-key>"
# NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"

# Balance (optional)
# If set, new signups during the window receive a Balance grant (USD, cents).
# SIGNUP_BONUS_AMOUNT_CENTS="500"
# SIGNUP_BONUS_START="2026-02-01T00:00:00Z"
# SIGNUP_BONUS_END="2026-02-29T23:59:59Z"
# SIGNUP_BONUS_DESCRIPTION="Welcome bonus"

# Images (optional)
# If Cloudinary proxying causes issues, set this to disable transformations and use Basta asset URLs directly.
# NEXT_PUBLIC_DISABLE_CLOUDINARY="true"
# Override Cloudinary cloud name (defaults to the value in lib/cloudinary.ts)
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="<your-cloud-name>"

# Lead uploads (valuation form)
# For serverless deployments, prefer storing uploads in the DB ("db"). For local dev you can use "fs".
# LEAD_UPLOAD_STORAGE="db" # "db" | "fs"
# LEAD_UPLOAD_DIR="./db/uploads/valuation-requests" # only used when LEAD_UPLOAD_STORAGE="fs"
